<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/rel"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.intel.fragmentlifecycle.MainActivity">

    <TableLayout
        android:layout_below="@+id/fragmentFrame"
        android:layout_width="match_parent"
        android:layout_marginTop="10dp"
        android:layout_height="wrap_content">

        <TableRow>
            <Button android:text="Add First"
                android:id="@+id/addfirst"/>

            <Button android:text="Add Second"
                android:id="@+id/addsecond"/>


        </TableRow>

        <TableRow>
            <Button android:text="Attach First"
                android:id="@+id/attach"/>

            <Button android:text="Detach First"
                android:id="@+id/detach"/>


        </TableRow>

        <TableRow>


            <Button android:text="Remove First"
                android:id="@+id/removeFirst"/>

            <Button android:text="Remove Second"
                android:id="@+id/removeSecond"/>


        </TableRow>

        <TableRow>

            <Button android:text="Replace With Second"
                android:id="@+id/replacewithsecond"/>

            <Button android:text="Replace With First"
                android:id="@+id/replacewithfirst"/>

        </TableRow>

        <TableRow>
            <Button android:text="Popup Dialog"
                android:layout_gravity="center"
                android:id="@+id/popupdialog"/>
        </TableRow>


    </TableLayout>

    <FrameLayout
        android:id="@+id/fragmentFrame"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        ></FrameLayout>
</RelativeLayout>
